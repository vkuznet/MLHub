<!DOCTYPE html>
<html lang="en">
<head>
    <title>MLHubi records</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{.Base}}/css/records.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/superkube@latest/dist/superkube.min.css">
</head>
<body>

<header>
    <h1 class="white">MLHub: models</h1>
    <div class="white">
        <a href="{{.Base}}/" class="button button-light-outline button-small button-round">Home</a>
        &nbsp;
        <a href="{{.Base}}/upload" class="button button-light-outline button-small button-round">Upload</a>
        &nbsp;
        <a href="{{.Base}}/apis" class="button button-light-outline button-small button-round">APIs</a>
        &nbsp;
        <a href="{{.Base}}/docs" class="button button-light-outline button-small button-round">Documentation</a>
    </div>
</header>

<section>
  <article>
{{range $rec := .Records}}
    <div class="record">
        Model: {{$rec.Model}}
        <br/>
        Type: {{$rec.Type}}
        <br/>
        Description: {{$rec.Description}}
        <hr/>
    </div> <!-- div record -->
{{end}}
  </article>
</section>

<footer>
  <p>MLHub &#169; Cornell University</p>
</footer>

</body>
</html>

